import {expect} from 'chai';

describe.skip('Bitmap', function () {
  it('gets bits', async () => {
    expect(true).to.be.false;
  });

  it('flips bits', async () => {
    expect(true).to.be.false;
  });
});
